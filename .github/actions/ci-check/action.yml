name: "CI checks"
description: "Makes sure all packages are buildable"
runs:
  using: "composite"
  steps:
    - name: Build
      run: npm run build
      shell: bash

    - name: Test
      run: npm run test --silent
      shell: bash
